
var options = {
    strings: ["designer", "developer", "maker"],
    backDelay: 1000,
    showCursor: false,
    smartBackspace: true,
    typeSpeed: 150,
    backSpeed: 100,
    loop: true
}

var typed = new Typed('.header-text-changing', options);
